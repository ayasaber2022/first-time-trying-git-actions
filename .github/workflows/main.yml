on:
   push:
     branches:
       - master





jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3   

      - name: "Login to Docker hub"
        run: docker login --username saber32 --password dckr_pat_ddcRpE5GV-DEIoWP3iJIVGzG6rM

      

